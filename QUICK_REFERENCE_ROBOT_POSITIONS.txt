ROBOT POSITION EXTRACTION - AIDE-MÉMOIRE
=========================================

PROBLÈME
--------
Les vidéos Robot extraites ne sont pas positionnées comme dans ScummVM.

CAUSE
-----
Les coordonnées X/Y ne sont PAS dans les fichiers .RBT mais passées
dynamiquement par les scripts SCI via kRobotOpen(robotId, plane, priority, X, Y, scale).

SOLUTION
--------
1. Patcher ScummVM pour logger les coordonnées
2. Jouer au jeu et capturer les logs
3. Parser les logs pour extraire les positions
4. Créer robot_positions.txt
5. Intégrer dans l'extracteur

COMMANDES
---------
# Automatique (recommandé):
./extract_robot_positions.sh

# Manuel:
cd ~/scummvm
patch -p1 < scummvm_robot_patch.diff
./configure --enable-debug --disable-all-engines --enable-engine=sci
make -j$(nproc)
./scummvm --debugflags=all --debuglevel=1 phantasmagoria_game 2>&1 | tee logs.txt
python3 parse_robot_logs.py logs.txt robot_positions.txt

FICHIERS
--------
📚 Docs:    README_ROBOT_POSITIONS.md (guide rapide)
🔧 Scripts: extract_robot_positions.sh, parse_robot_logs.py
💻 Code:    robot_position_loader_example.cpp
📊 Résumé:  ROBOT_POSITIONS_SUMMARY.txt

FORMAT robot_positions.txt
--------------------------
1000 150 143
230 180 160
91 175 150

AIDE
----
cat README_ROBOT_POSITIONS.md       # Guide complet
cat ROBOT_POSITIONS_SUMMARY.txt     # Résumé exécutif
cat FILE_INDEX.md                   # Index de tous les fichiers
