--- a/src/robot_decoder/rbt_parser.cpp
+++ b/src/robot_decoder/rbt_parser.cpp
@@ -775,6 +775,10 @@ bool RobotParser::parse() {
             
             int8_t bufferIndex = (audioPos % 4) ? 1 : 0;
             
+            // LOG POSITIONS
+            printf("AUDIO_POS: frame=%zu pos=%d size=%u channel=%s\n",
+                   i, audioPos, actualSize, bufferIndex ? "ODD" : "EVEN");
+            
             if (VERBOSE_AUDIO) {
                 std::fprintf(stderr, "Frame %zu: audioPos=%d bufferIndex=%d (%s) compSize=%u\n",
                             i, audioPos, bufferIndex, bufferIndex ? "ODD" : "EVEN", actualSize);
